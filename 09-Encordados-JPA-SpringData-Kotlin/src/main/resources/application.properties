# URL jdbc de conexión a la base de datos
# No usamos MySQL o MariaDB. Vamos a usar esta base de datos embebida
# Para evitar llevárnosla a cualquier lado con su fichero SQL
# En fichero embebida o en memoria, tú eliges
# spring.datasource.url=jdbc:h2:./tenistas
spring.datasource.url=jdbc:h2:mem:tenistas;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;
# Usuario y contraseña de la base de datos
spring.datasource.username=sa
#spring.datasource.password=password
# Habilitamos la consola de H2
http=//localhost:{server.port}/h2-console
# En nuestro caso http://localhost:6969/h2-console
spring.h2.console.enabled=true
# Habilitamos los mensajes sql en el log para ver las consultas
spring.jpa.show-sql=true
# Busca ufichero de base de datos llamado data.sql
# spring.jpa.defer-datasource-initialization=true


